[phases.setup]
  nixPkgs = ["openjdk17", "maven"]

[phases.build]
  cmds = [
    "mvn clean package -DskipTests",
    "ls -la target/"
  ]

[phases.start]
  cmd = "java -Xmx512m -jar target/backend-nextread-0.0.1-SNAPSHOT.jar"

[variables]
  MAVEN_OPTS = "-Xmx512m"
  JAVA_TOOL_OPTIONS = "-Xmx512m"